<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" type="text/css" href="style.css">
    <title>Most Common Word</title>
  </head>
  <body>
    <div>
    <h1>Welcome to Melody's API</h1>
    <form action="/" method="post" encType="multipart/form-data">
    
      <label for="files" class="btn btn-primary btn-lg">Select file</label>
      <input type="file"  style="visibility:hidden;" id="files" name="textFile" />
      <button type="submit" class="btn btn-primary btn-lg"  onsubmit="onSubmit()">Upload</button>
    </form>
    <%if (typeof error !== 'undefined') { %>
    <h3><%= error %></h3>
    <% } %>

    <p>
        <%if (typeof result !== 'undefined') { %>
          <h2><%= result %></h2>
          <% } %>
    </p>
    <p>
        <%if (typeof word !== 'undefined' && typeof total !== 'undefined') { %>
          <span>Most Common Word in this text file is " <%= word %> " and it used " <%=
            total %> " times</span>
          <% } %>
    </p>
   
  </div>
  </body>
  <script>
    function onSubmit(e){
      e.preventDefault();
    }
  </script>
</html>
